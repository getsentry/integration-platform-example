import React from 'react';
import styled from '@emotion/styled';

const Header = () => <header>testing</header>;

const StyledHeader = styled(Header)`
  background: blue;
  border: 2px solid red;
`;

export default StyledHeader;
